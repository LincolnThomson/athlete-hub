import { Link } from 'wouter';
import { AppShell, Button } from '@/components/athlete-ui';

export default function Launch() {
  return <AppShell><div className="atmosphere min-h-[calc(100dvh-1px)]"><div className="mx-auto max-w-2xl px-5 py-7 md:px-10 md:py-10 text-center"><h1 className="font-display text-6xl font-black uppercase leading-tight">Athlete Hub is Live</h1><p className="mt-4 text-lg text-[hsl(var(--muted-foreground))]">Your basketball career starts now.</p><div className="mt-12 space-y-4"><Link href="/dashboard"><Button className="w-full py-4">Enter Dashboard</Button></Link><Link href="/collection"><Button variant="outline" className="w-full py-4">View Collection</Button></Link></div><div className="mt-12 rounded-2xl border border-[hsl(var(--border))] p-8"><p className="font-mono text-[10px] uppercase tracking-widest text-[hsl(var(--primary))]">Launch Stats</p><div className="mt-6 grid grid-cols-3 gap-4"><div><p className="font-display text-3xl font-black">0</p><p className="text-xs text-[hsl(var(--muted-foreground))]">Players</p></div><div><p className="font-display text-3xl font-black">0</p><p className="text-xs text-[hsl(var(--muted-foreground))]">Games</p></div><div><p className="font-display text-3xl font-black">0</p><p className="text-xs text-[hsl(var(--muted-foreground))]">Cards</p></div></div></div><p className="mt-8 text-xs text-[hsl(var(--muted-foreground))]">Built with speed. Shipped with heart. 🏀</p></div></div></AppShell>;
}
