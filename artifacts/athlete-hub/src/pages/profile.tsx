import { Edit3, MapPin, ShieldCheck, Star, UserRound } from 'lucide-react';
import { useState } from 'react';
import { player, achievements } from '@/data/mock-data';
import { AchievementCard, AppShell, Button, PlayerCard, StatCard } from '@/components/athlete-ui';

export default function Profile() {
  const [notice, setNotice] = useState('');
  return <AppShell><div className="mx-auto max-w-7xl px-5 py-7 md:px-10 md:py-10"><header className="flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="font-mono text-[10px] uppercase tracking-[.2em] text-[hsl(var(--primary))]">Player profile</p><h1 className="mt-2 font-display text-6xl font-black uppercase leading-[.82]">Your identity.<br /><span className="text-[hsl(var(--primary))]">Your game.</span></h1></div><Button variant="outline" onClick={() => setNotice('Profile editing is ready for the next season update.')} data-testid="button-edit-profile"><Edit3 size={16} /> Edit profile</Button></header>{notice && <button onClick={() => setNotice('')} className="mt-4 rounded-lg bg-[hsl(var(--secondary))] px-4 py-3 text-xs font-bold text-[hsl(var(--secondary-foreground))]" data-testid="button-dismiss-profile-notice">{notice}</button>}<div className="mt-8 grid gap-5 xl:grid-cols-[.8fr_1.2fr]"><section className="relative overflow-hidden rounded-2xl bg-[hsl(var(--secondary))] p-6 text-[hsl(var(--secondary-foreground))] sm:p-8"><div className="absolute -right-16 -top-20 h-64 w-64 rounded-full border border-white/10" /><div className="relative flex items-start gap-5"><div className="grid h-24 w-24 shrink-0 place-items-center rounded-2xl bg-[#e5b08e] text-[#203b4e]"><UserRound size={62} strokeWidth={1.1} /></div><div><div className="flex items-center gap-2"><h2 className="font-display text-4xl font-black uppercase">{player.name}</h2><ShieldCheck size={18} className="text-[hsl(var(--accent))]" /></div><p className="font-mono text-xs text-white/55">{player.username}</p><div className="mt-4 flex flex-wrap gap-2 text-[10px] font-bold uppercase tracking-wider"><span className="rounded bg-white/10 px-2 py-1">{player.position}</span><span className="rounded bg-white/10 px-2 py-1">{player.grade}</span><span className="rounded bg-white/10 px-2 py-1">{player.team}</span></div></div></div><div className="relative mt-9 grid grid-cols-2 gap-4 border-t border-white/10 pt-5"><div><p className="font-mono text-[9px] uppercase tracking-widest text-white/45">Home court</p><p className="mt-1 flex items-center gap-1 text-sm font-bold"><MapPin size={13} className="text-[hsl(var(--primary))]" />Seattle, WA</p></div><div><p className="font-mono text-[9px] uppercase tracking-widest text-white/45">Member since</p><p className="mt-1 text-sm font-bold">August 2023</p></div></div></section><section className="flex flex-col justify-between rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-6 sm:p-8"><div className="flex items-start justify-between"><div><p className="font-mono text-[10px] uppercase tracking-[.2em] text-[hsl(var(--muted-foreground))]">Overall rating</p><p className="mt-2 font-display text-8xl font-black leading-[.72] text-[hsl(var(--primary))]">{player.ovr}</p></div><div className="flex gap-1 pt-1">{Array.from({ length: 5 }).map((_, i) => <Star key={i} size={18} className={i < player.stars ? 'fill-[hsl(var(--accent))] text-[hsl(var(--accent))]' : 'text-[hsl(var(--border))]'} />)}</div></div><div className="mt-10 grid grid-cols-3 gap-2 border-t border-[hsl(var(--border))] pt-5"><StatCard label="PPG" value={player.ppg} /><StatCard label="RPG" value={player.rpg} /><StatCard label="APG" value={player.apg} /></div></section></div><div className="mt-5 grid gap-5 lg:grid-cols-[.85fr_1.15fr]"><section className="rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-5 sm:p-7"><div className="flex items-center justify-between"><div><p className="font-mono text-[10px] uppercase tracking-[.2em] text-[hsl(var(--muted-foreground))]">Signature</p><h2 className="mt-1 font-display text-3xl font-bold uppercase">Your card</h2></div><span className="font-mono text-[10px] text-[hsl(var(--muted-foreground))]">1 / 1</span></div><div className="mt-6 flex justify-center"><PlayerCard compact /></div></section><section className="rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-5 sm:p-7"><div className="flex items-center justify-between"><div><p className="font-mono text-[10px] uppercase tracking-[.2em] text-[hsl(var(--muted-foreground))]">The resume</p><h2 className="mt-1 font-display text-3xl font-bold uppercase">Achievements</h2></div><span className="rounded-full bg-[hsl(var(--accent))] px-2 py-1 font-mono text-[10px] font-bold">{achievements.filter(a => a.unlocked).length} / {achievements.length}</span></div><div className="mt-6 grid gap-3 sm:grid-cols-2">{achievements.map(item => <AchievementCard key={item.id} item={item} />)}</div></section></div></div></AppShell>;
}