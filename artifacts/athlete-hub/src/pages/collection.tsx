import { Boxes, Filter, PackageOpen, Search, Sparkles } from 'lucide-react';
import { useMemo, useState } from 'react';
import { cards, player, type Rarity } from '@/data/mock-data';
import { AppShell, CollectionCard, PackPreview } from '@/components/athlete-ui';

const filters: Array<'All' | Rarity> = ['All', 'Common', 'Rare', 'Epic', 'Elite', 'Legendary'];

export default function Collection() {
  const [active, setActive] = useState<'All' | Rarity>('All');
  const [searchOpen, setSearchOpen] = useState(false);
  const [query, setQuery] = useState('');
  const filtered = useMemo(() => (active === 'All' ? cards : cards.filter(card => card.rarity === active)).filter(card => `${card.name} ${card.subtitle}`.toLowerCase().includes(query.toLowerCase())), [active, query]);
  return <AppShell><div className="atmosphere min-h-[calc(100dvh-1px)]"><div className="mx-auto max-w-7xl px-5 py-7 md:px-10 md:py-10">
    <header className="flex flex-col justify-between gap-6 lg:flex-row lg:items-end"><div><div className="flex items-center gap-2 font-mono text-[10px] uppercase tracking-[.2em] text-[hsl(var(--primary))]"><Boxes size={13} /> Career collection · Preview</div><h1 className="mt-3 font-display text-6xl font-black uppercase leading-[.82] sm:text-8xl">Keep the<br /><span className="text-gradient">receipts.</span></h1><p className="mt-5 max-w-md text-sm leading-relaxed text-[hsl(var(--muted-foreground))]">Every card is a moment from your journey. The more you play, the more of your story you get to hold.</p></div><div className="surface-elevated rounded-2xl border border-[hsl(var(--primary))]/25 p-4 sm:min-w-[260px]"><div className="flex items-center gap-3"><Boxes className="text-[hsl(var(--primary))]" size={24} /><div><p className="font-display text-4xl font-black leading-none">{player.collectionCount} <span className="font-sans text-sm font-bold text-[hsl(var(--muted-foreground))]">/ 100 CARDS</span></p><p className="mt-1 font-mono text-[9px] uppercase tracking-widest text-[hsl(var(--muted-foreground))]">Collection progress · mock</p></div></div><div className="mt-4 h-2 overflow-hidden rounded-full bg-[hsl(var(--muted))]"><div className="animate-progress h-full rounded-full bg-[linear-gradient(90deg,hsl(var(--primary)),hsl(var(--secondary-accent)))]" style={{ width: `${player.collectionCount}%` }} /></div></div></header>
    <div className="mt-10 grid gap-5 lg:grid-cols-[1.35fr_.65fr]"><div className="rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--card))]/60 p-4 sm:p-5"><div className="flex flex-col gap-4 border-b border-[hsl(var(--border))] pb-4"><div className="flex items-center gap-2 overflow-x-auto pb-1">{filters.map(filter => <button key={filter} onClick={() => setActive(filter)} className={`shrink-0 rounded-full px-3 py-2 font-mono text-[10px] font-bold uppercase tracking-wider transition-colors ${active === filter ? 'bg-[hsl(var(--primary))] text-[hsl(var(--primary-foreground))]' : 'bg-[hsl(var(--muted))] text-[hsl(var(--muted-foreground))] hover:bg-[hsl(var(--secondary-accent))]/20 hover:text-[hsl(var(--foreground))]'}`} data-testid={`button-filter-${filter.toLowerCase()}`}><span className="mr-1.5">{filter === 'All' ? <Filter className="inline" size={11} /> : null}</span>{filter}</button>)}</div><div className="flex items-center justify-between gap-4"><p className="font-mono text-[10px] uppercase tracking-[.16em] text-[hsl(var(--muted-foreground))]">{filtered.length} cards showing</p>{searchOpen ? <label className="flex items-center gap-2 border-b border-[hsl(var(--primary))] px-1 py-1"><Search size={14} className="text-[hsl(var(--primary))]" /><input autoFocus value={query} onChange={event => setQuery(event.target.value)} placeholder="Search cards" className="w-32 bg-transparent text-xs outline-none placeholder:text-[hsl(var(--muted-foreground))]" data-testid="input-search-collection" /></label> : <button onClick={() => setSearchOpen(true)} className="inline-flex items-center gap-2 text-xs font-bold text-[hsl(var(--muted-foreground))]" aria-label="Search collection" data-testid="button-search-collection"><Search size={14} /> Search</button>}</div></div>{filtered.length > 0 ? <div className="animate-stagger mt-6 grid grid-cols-2 gap-3 sm:grid-cols-3 lg:grid-cols-4">{filtered.map(card => <CollectionCard key={card.id} card={card} />)}</div> : <div className="mt-6 rounded-2xl border border-dashed border-[hsl(var(--border))] bg-[hsl(var(--secondary))]/40 px-5 py-20 text-center"><Boxes size={32} className="mx-auto text-[hsl(var(--primary))]" /><h2 className="mt-4 font-display text-3xl font-bold uppercase">Your career starts here.</h2><p className="mx-auto mt-2 max-w-sm text-sm text-[hsl(var(--muted-foreground))]">Start playing to earn your first card. This collection is a preview while the reward system is in development.</p></div>}</div><div className="space-y-5"><PackPreview /><div className="rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--card))]/60 p-5"><div className="flex items-center gap-2"><Sparkles size={17} className="text-[hsl(var(--accent))]" /><p className="font-mono text-[10px] uppercase tracking-wider text-[hsl(var(--muted-foreground))]">Binder note</p></div><p className="mt-3 font-display text-3xl font-black uppercase">Every season has a story.</p><p className="mt-2 text-sm leading-relaxed text-[hsl(var(--muted-foreground))]">Cards will eventually represent seasons, achievements, milestones, performances, and special moments.</p><div className="mt-4 flex items-center gap-2 font-mono text-[9px] uppercase tracking-wider text-[hsl(var(--secondary-accent))]"><PackageOpen size={13} /> Pack opening · Coming Soon</div></div></div></div>
  </div></div></AppShell>;
}