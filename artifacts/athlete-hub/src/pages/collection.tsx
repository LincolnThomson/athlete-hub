import { Boxes, Filter, Search } from 'lucide-react';
import { useMemo, useState } from 'react';
import { cards, type Rarity } from '@/data/mock-data';
import { AppShell, CollectionCard } from '@/components/athlete-ui';

const filters: Array<'All' | Rarity> = ['All', 'Common', 'Rare', 'Epic', 'Elite', 'Legendary'];

export default function Collection() {
  const [active, setActive] = useState<'All' | Rarity>('All');
  const [searchOpen, setSearchOpen] = useState(false);
  const [query, setQuery] = useState('');
  const filtered = useMemo(() => (active === 'All' ? cards : cards.filter(card => card.rarity === active)).filter(card => `${card.name} ${card.subtitle}`.toLowerCase().includes(query.toLowerCase())), [active, query]);
  return <AppShell><div className="mx-auto max-w-7xl px-5 py-7 md:px-10 md:py-10"><header className="flex flex-col justify-between gap-6 sm:flex-row sm:items-end"><div><p className="font-mono text-[10px] uppercase tracking-[.2em] text-[hsl(var(--primary))]">Career collection</p><h1 className="mt-2 font-display text-6xl font-black uppercase leading-[.82]">Keep the<br /><span className="text-[hsl(var(--primary))]">receipts.</span></h1><p className="mt-4 max-w-md text-sm text-[hsl(var(--muted-foreground))]">Every card is a moment from your journey. The more you play, the more of your story you get to hold.</p></div><div className="flex items-center gap-3 rounded-xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-3"><Boxes className="text-[hsl(var(--primary))]" size={22} /><div><p className="font-display text-3xl font-black leading-none">18 <span className="font-sans text-sm font-bold text-[hsl(var(--muted-foreground))]">/ 100 CARDS</span></p><p className="mt-1 font-mono text-[9px] uppercase tracking-widest text-[hsl(var(--muted-foreground))]">Collection complete</p></div></div></header><div className="mt-10 flex flex-col gap-4 border-b border-[hsl(var(--border))] pb-4"><div className="flex items-center gap-2 overflow-x-auto pb-1">{filters.map(filter => <button key={filter} onClick={() => setActive(filter)} className={`shrink-0 rounded-full px-3 py-2 font-mono text-[10px] font-bold uppercase tracking-wider transition-colors ${active === filter ? 'bg-[hsl(var(--secondary))] text-[hsl(var(--secondary-foreground))]' : 'bg-[hsl(var(--muted))] text-[hsl(var(--muted-foreground))] hover:bg-[hsl(var(--accent))] hover:text-[hsl(var(--accent-foreground))]'}`} data-testid={`button-filter-${filter.toLowerCase()}`}><span className="mr-1.5">{filter === 'All' ? <Filter className="inline" size={11} /> : null}</span>{filter}</button>)}</div><div className="flex items-center justify-between gap-4"><p className="font-mono text-[10px] uppercase tracking-[.16em] text-[hsl(var(--muted-foreground))]">{filtered.length} cards showing</p>{searchOpen ? <label className="flex items-center gap-2 border-b border-[hsl(var(--primary))] px-1 py-1"><Search size={14} className="text-[hsl(var(--primary))]" /><input autoFocus value={query} onChange={event => setQuery(event.target.value)} placeholder="Search cards" className="w-32 bg-transparent text-xs outline-none placeholder:text-[hsl(var(--muted-foreground))]" data-testid="input-search-collection" /></label> : <button onClick={() => setSearchOpen(true)} className="inline-flex items-center gap-2 text-xs font-bold text-[hsl(var(--muted-foreground))]" aria-label="Search collection" data-testid="button-search-collection"><Search size={14} /> Search</button>}</div></div>{filtered.length > 0 ? <div className="mt-6 grid grid-cols-2 gap-3 sm:grid-cols-3 lg:grid-cols-4 xl:grid-cols-5">{filtered.map(card => <CollectionCard key={card.id} card={card} />)}</div> : <div className="mt-6 rounded-2xl border border-dashed border-[hsl(var(--border))] bg-[hsl(var(--card))] px-5 py-20 text-center"><Boxes size={32} className="mx-auto text-[hsl(var(--muted-foreground))]" /><h2 className="mt-4 font-display text-3xl font-bold uppercase">No {active} cards yet</h2><p className="mt-2 text-sm text-[hsl(var(--muted-foreground))]">Keep playing. Your next card is somewhere in the work.</p></div>}</div></AppShell>;
}